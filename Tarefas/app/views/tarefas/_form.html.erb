<%= form_with(model: tarefa, local: true) do |form| %>
  <% if tarefa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarefa.errors.count, "error") %> prohibited this tarefa from being saved:</h2>

      <ul>
      <% tarefa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nome %>
    <%= form.text_field :Nome, id: :tarefa_Nome %>
  </div>

  <div class="field">
    <%= form.label :Descricao %>
    <%= form.text_field :Descricao, id: :tarefa_Descricao %>
  </div>

  <div class="field">
    <%= form.label :Prioridade %>
    <%= form.number_field :Prioridade, id: :tarefa_Prioridade %>
  </div>

  <div class="field">
    <%= form.label :DataPrazo %>
    <%= form.number_field :DataPrazo, id: :tarefa_DataPrazo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
